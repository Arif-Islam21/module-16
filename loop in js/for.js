/**
 * for ( ; ; ){
 *  loop statement
 * }
 * first condition: loop variable declaration
 * second condition: loop condition declaration
 * third condition : loop variable change
 */
for (let num = 0; num < 5; num++) {
  console.log(num);
}
